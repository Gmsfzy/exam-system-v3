# 在线考试系统 — 说明文档

> **版本**：v3.0
> **技术栈**：Python + Flask + SQLite + DashScope（通义千问大模型）
> **架构**：Flask Blueprints + 分层架构
> **最后更新**：2026-09

---

## 一、系统概述

本系统是一个功能完整的在线考试平台，面向教师和学生两类角色，支持从出题、组卷、考试、评分到成绩分析的全流程闭环。系统集成了通义千问大模型（DashScope），提供 AI 智能出题、AI 自动评分简答题、AI 生成试题解析等能力，大幅提升教学效率。

### 核心亮点

- **五种题型**：单选题、多选题、判断题、简答题、编程题
- **AI 智能出题**：输入主题和难度，一键批量生成题目并配套解析入库
- **AI 自动评分**：简答题提交后由大模型实时语义评分，无需人工批改
- **编程题自动评测**：子进程隔离执行学生代码，自动对比测试用例输出
- **防作弊机制**：切屏检测、失焦记录，行为日志写入数据库
- **完整评分管理**：客观题自动判分 + 主观题 AI/人工双重评分 + 教师复审
- **考试状态管控**：教师可启用/禁用考试、设置起止时间、随机打乱题目顺序
- **试题解析系统**：AI 单题/批量生成解析，学生考后可查看答案与解析
- **敏感信息保护**：API Key 和 SECRET_KEY 通过 `.env` 环境变量注入，不暴露在源码中

---

## 二、技术架构

| 层级 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Flask | Python 轻量级 Web 框架，Blueprints 模块化路由 |
| 数据库 | SQLite 3 | 零配置嵌入式关系数据库，数据文件 `exam_system.db` |
| 前端 UI | Bootstrap 5.3 | 响应式布局，配合 Bootstrap Icons / Font Awesome |
| 富文本编辑 | Summernote 0.8.18 | 题目内容支持富文本编辑 |
| 代码高亮 | Prism.js | 考试结果页编程题代码高亮展示 |
| AI 大模型 | DashScope（通义千问 qwen-max） | 智能出题、简答题评分、生成解析 |
| 密码安全 | Werkzeug `generate_password_hash` / `check_password_hash` | PBKDF2 哈希加盐 |
| 环境变量 | python-dotenv | 从 `.env` 文件加载敏感配置 |
| 日志系统 | Python `logging` | 统一时间戳格式，记录关键操作与异常 |

### 架构分层

```
exam_system_v3/
├── config.py        # 配置集中管理（从 .env 加载敏感信息）
├── models/          # 数据层（所有 SQL 隔离）
│   ├── db.py        # 数据库连接与初始化
│   ├── user.py      # 用户模型
│   ├── question.py  # 题目模型
│   ├── exam.py      # 考试模型
│   ├── exam_record.py  # 考试记录模型
│   ├── grading.py   # 评分记录模型
│   └── image.py     # 图片附件模型
├── routes/          # 路由层（按角色拆分蓝图）
│   ├── auth.py      # 认证蓝图（auth_bp）
│   ├── main.py      # 主页面蓝图（main_bp）
│   ├── teacher.py   # 教师功能蓝图（teacher_bp）
│   ├── student.py   # 学生功能蓝图（student_bp）
│   └── common.py    # 公共功能蓝图（common_bp）
├── services/        # 业务服务层
│   ├── ai_service.py    # AI 调用（出题/评分/解析）
│   └── evaluator.py     # 编程题评测
├── utils/           # 工具层
│   ├── decorators.py    # 登录/角色装饰器
│   └── answer.py        # 答案判断工具
└── templates/       # Jinja2 HTML 模板
```

---

## 三、项目结构

```
exam_system_v3/
├── app.py                  # 主应用入口（注册蓝图、初始化数据库）
├── config.py               # 配置类（从环境变量读取敏感信息）
├── exam_system.db          # SQLite 数据库文件（运行时自动创建）
├── .env                    # 环境变量配置（敏感信息，不纳入版本控制）
├── .gitignore              # Git 忽略规则
├── 说明文档.md              # 本文档
├── models/                 # 数据层
│   ├── __init__.py
│   ├── db.py
│   ├── user.py
│   ├── question.py
│   ├── exam.py
│   ├── exam_record.py
│   ├── grading.py
│   └── image.py
├── routes/                 # 路由层（Blueprints）
│   ├── __init__.py
│   ├── auth.py
│   ├── main.py
│   ├── teacher.py
│   ├── student.py
│   └── common.py
├── services/               # 业务服务层
│   ├── __init__.py
│   ├── ai_service.py
│   └── evaluator.py
├── utils/                  # 工具层
│   ├── __init__.py
│   ├── decorators.py
│   └── answer.py
└── templates/              # Jinja2 HTML 模板
    ├── base.html
    ├── index.html
    ├── login.html
    ├── register.html
    ├── teacher_dashboard.html
    ├── student_dashboard.html
    ├── add_question.html
    ├── manage_questions.html
    ├── edit_question.html
    ├── create_exam.html
    ├── take_exam.html
    ├── exam_result.html
    ├── exam_results.html
    ├── grade_subjective_questions.html
    └── ai_generate.html
```

---

## 四、数据库设计

系统共 7 张表，启动时由 `init_db()` 自动创建并兼容旧表迁移。

### 4.1 users — 用户表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | TEXT UNIQUE | 用户名（唯一） |
| password | TEXT | PBKDF2 哈希密码 |
| role | TEXT | 角色：`teacher` 或 `student` |
| created_at | TIMESTAMP | 注册时间 |

### 4.2 questions — 题目表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| teacher_id | INTEGER FK | 出题教师 ID |
| question_text | TEXT | 题目内容（支持富文本 HTML） |
| option_a ~ option_f | TEXT | 选项 A~F（多选题最多6个选项） |
| correct_answer | TEXT | 正确答案（多选用逗号分隔如 `A,B,C`） |
| question_type | TEXT | 题型：`multiple_choice` / `multiple_select` / `true_false` / `short_answer` / `programming` |
| subject | TEXT | 科目/专业课 |
| difficulty | TEXT | 难度：`easy` / `medium` / `hard` |
| points | INTEGER | 分值（默认1分） |
| test_cases | TEXT | 编程题测试用例（JSON 数组） |
| solution_code | TEXT | 编程题参考答案代码 |
| explanation | TEXT | 题目解析（AI 生成或手写） |
| created_at | TIMESTAMP | 创建时间 |

### 4.3 exams — 考试表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| title | TEXT | 考试名称 |
| subject | TEXT | 科目 |
| teacher_id | INTEGER FK | 创建教师 ID |
| duration_minutes | INTEGER | 考试时长（分钟） |
| start_time | TIMESTAMP | 开始时间（可为空表示立即开始） |
| end_time | TIMESTAMP | 结束时间（可为空表示不限） |
| status | TEXT | 状态：`active`（启用）/ `inactive`（禁用） |
| anti_cheat_enabled | BOOLEAN | 是否启用防作弊 |
| shuffle_questions | BOOLEAN | 是否随机打乱题目顺序 |
| created_at | TIMESTAMP | 创建时间 |

### 4.4 exam_questions — 考试题目关联表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| exam_id | INTEGER FK | 考试 ID |
| question_id | INTEGER FK | 题目 ID |

### 4.5 exam_records — 考试记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| exam_id | INTEGER FK | 考试 ID |
| student_id | INTEGER FK | 学生 ID |
| start_time | TIMESTAMP | 开始答题时间 |
| submit_time | TIMESTAMP | 提交时间 |
| score | INTEGER | 总得分（主观题未评完时为 NULL） |
| total_questions | INTEGER | 总题数 |
| answers | TEXT | 学生答案（JSON 格式，key 为题目 ID） |
| status | TEXT | 状态：`in_progress` / `submitted` |
| anti_cheat_logs | TEXT | 防作弊日志（JSON 数组） |

### 4.6 grading_records — 评分记录表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| exam_record_id | INTEGER FK | 考试记录 ID |
| question_id | INTEGER FK | 题目 ID |
| student_answer | TEXT | 学生作答内容 |
| teacher_score | INTEGER | 教师/AI 给的分数（`grader_id=-1` 表示 AI 评分） |
| auto_score | INTEGER | 编程题自动评测分数 |
| max_score | INTEGER | 该题满分 |
| grader_id | INTEGER | 评分者 ID（`-1` = AI，正整数 = 教师用户 ID） |
| graded_at | TIMESTAMP | 评分时间 |

### 4.7 image_attachments — 图片附件表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| question_id | INTEGER FK | 关联题目 ID |
| exam_record_id | INTEGER FK | 关联考试记录 ID |
| filename | TEXT | 文件名 |
| data | BLOB | 图片二进制数据 |
| uploaded_at | TIMESTAMP | 上传时间 |

---

## 五、功能模块详解

### 5.1 用户系统

| 功能 | 端点 | 说明 |
|------|------|------|
| 首页 | `main.index` | 欢迎页，已登录则显示进入仪表板按钮 |
| 注册 | `auth.register` | 填写用户名、密码、选择角色（教师/学生） |
| 登录 | `auth.login` | 用户名 + 密码验证，登录后写入 session |
| 登出 | `auth.logout` | 清除 session，跳转首页 |
| 仪表板 | `main.dashboard` | 按角色分流：教师 → 教师仪表盘，学生 → 学生仪表盘 |

**安全机制**：
- 密码使用 Werkzeug 的 `generate_password_hash` 进行 PBKDF2 加盐哈希，数据库不存明文
- `login_required` 装饰器：未登录自动跳转登录页
- `role_required` 装饰器：角色不符则拒绝访问并提示

### 5.2 教师功能

#### 5.2.1 题库管理

| 功能 | 端点 | 说明 |
|------|------|------|
| 添加题目 | `teacher.add_question` | 手动添加，支持5种题型，富文本编辑器 |
| 管理题库 | `teacher.manage_questions` | 按专业课/题型筛选，支持编辑/删除/AI生成解析 |
| 编辑题目 | `teacher.edit_question` | 编辑已有题目（仅允许编辑自己创建的） |
| 删除题目 | `teacher.delete_question` | 删除题目（仅允许删除自己创建的） |
| AI 生成解析 | `teacher.generate_explanation` | 调用千问为指定题目生成详细解析 |
| 批量生成解析 | `teacher.generate_all_explanations` | 一键为所有缺解析的题目生成解析 |

**题型字段差异**：
- **单选题/多选题/判断题**：需填写选项（A~F）和正确答案
- **简答题**：需填写参考答案/得分要点（存入 `correct_answer` 字段）
- **编程题**：需填写测试用例（JSON 格式）和参考代码

#### 5.2.2 考试管理

| 功能 | 端点 | 说明 |
|------|------|------|
| 创建考试 | `teacher.create_exam` | 设置名称/科目/时长/起止时间/防作弊/随机顺序，勾选题目组卷 |
| 启用/禁用考试 | `teacher.toggle_exam_status` | 切换考试 `active`/`inactive` 状态 |
| 查看考试结果 | `teacher.exam_results` | 统计总人数/平均分/最高分/最低分，列出每位学生成绩 |
| 清空考试成绩 | `teacher.clear_exam_scores` | 删除该考试所有成绩记录和评分记录 |
| 删除考试 | `teacher.delete_exam` | 级联删除考试及关联的题目关联、成绩记录、评分记录 |

#### 5.2.3 评分复审

| 功能 | 端点 | 说明 |
|------|------|------|
| 评分复审 | `teacher.grade_subjective_questions` | 查看待评分主观题/编程题，教师可调整 AI 评分或手动打分 |

**评分流程**：
1. 客观题（单选/多选/判断）→ 自动判分
2. 编程题 → 自动评测（测试用例通过率），部分需人工复审
3. 简答题 → AI 实时评分（0~10 分映射到实际分值），AI 失败则标记待人工
4. 教师在评分复审页面可查看 AI 评分结果并调整

#### 5.2.4 AI 智能出题

| 功能 | 端点 | 说明 |
|------|------|------|
| AI 出题 | `teacher.ai_generate` | 选择题型/输入主题/难度/数量，一键生成入库 |

**支持的 AI 出题题型**：
- 单选题：生成题目 + 4个选项 + 正确答案 + 解析
- 多选题：生成题目 + 多个选项 + 多个正确答案 + 解析
- 判断题：生成陈述 + 正误判断 + 解析
- 简答题：生成题目 + 参考答案 + 解析
- 编程题：生成题目描述 + 测试用例 + 参考代码 + 解析

### 5.3 学生功能

| 功能 | 端点 | 说明 |
|------|------|------|
| 学生仪表盘 | `main.dashboard` | 显示进行中/即将开始/历史考试三个区域 |
| 参加考试 | `student.take_exam` | 进入答题页面，倒计时计时，防作弊监控 |
| 提交考试 | `student.submit_exam` | 提交所有答案，自动评分客观题，AI评分简答题，编程题自动评测 |
| 查看考试结果 | `student.view_exam_result` | 查看每题作答、得分、正确答案与解析（考试结束后可见） |
| 防作弊事件上报 | `student.cheating_detected` | 前端检测到切屏/失焦等行为时上报 |

**考试结果解析可见性规则**：
- 考试状态为 `inactive`（教师已停止）→ 可查看解析
- 考试结束时间已过 → 可查看解析
- 其他情况 → 仅显示作答记录，不显示答案与解析

**安全校验**：`student.view_exam_result` 路由强制绑定当前登录用户 ID，防止越权访问他人成绩。

### 5.4 防作弊机制

- **前端监控**：检测浏览器标签页切换（`visibilitychange`）、窗口失焦（`blur`）
- **行为上报**：检测到异常行为时，通过 AJAX POST 到 `student.cheating_detected`
- **日志存储**：行为记录追加到 `exam_records.anti_cheat_logs` 字段（JSON 数组）
- **防御性编程**：日志写入时处理记录不存在、JSON 解析失败等边界情况

### 5.5 编程题自动评测

评测函数 `evaluate_programming_solution(student_code, test_cases_json)` 的工作流程：

1. 解析测试用例 JSON（每个用例包含 `input` 和 `expected_output`）
2. 将学生代码写入临时 `.py` 文件
3. 使用 `subprocess.run` 隔离执行：
   - `-S` 参数：不导入 site 模块，减少可用模块
   - `timeout=5`：5 秒超时防止死循环
   - `env=restricted_env`：受限环境变量，移除敏感信息
4. 对比标准输出与期望输出（精确匹配）
5. 返回 `(通过数, 总用例数)`，按比例计算得分
6. 清理临时文件

---

## 六、AI 集成说明

### 6.1 模型配置

- **模型**：通义千问 `qwen-max`（DashScope 平台）
- **API Key 管理**：通过 `.env` 文件配置，使用 `python-dotenv` 加载

### 6.2 AI 功能清单

| 功能 | 调用时机 | Prompt 策略 |
|------|----------|-------------|
| 智能出题 | 教师在 AI 出题页提交表单 | 根据题型构建不同的 JSON 格式 Prompt，要求模型严格输出 JSON |
| 简答题评分 | 学生提交考试时实时调用 | 角色=阅卷官，0~10 分语义评分，要求输出 JSON `{"score":, "comment":}` |
| 生成单题解析 | 教师在题库管理页点击"生成解析" | 根据题型构建解析 Prompt，要求输出纯文本 |
| 批量生成解析 | 教师点击"批量生成解析" | 逐题调用，跳过已有解析的题目 |

### 6.3 AI 输出清洗

- 去除 Markdown 代码块标记（` ```json ` / ` ``` `）
- JSON 解析失败时记录原始输出到日志，提示教师重试
- 简答题 AI 评分返回 `None` 时，自动标记为待人工复核

---

## 七、环境配置与部署

### 7.1 环境要求

- Python 3.10+
- 依赖包：`flask`、`werkzeug`、`dashscope`、`python-dotenv`

### 7.2 安装步骤

```bash
# 1. 克隆项目
git clone <仓库地址>
cd exam_system_v3

# 2. 安装依赖
pip install flask werkzeug dashscope python-dotenv

# 3. 配置环境变量
# 复制 .env.example 为 .env（或手动创建），填入你的配置
```

### 7.3 `.env` 配置文件

在项目根目录创建 `.env` 文件：

```env
# Flask 会话密钥（生产环境请使用强随机字符串）
SECRET_KEY=your-strong-secret-key-here

# DashScope API Key（用于 AI 出题/评分/解析）
DASHSCOPE_API_KEY=your-dashscope-api-key-here
```

> **重要**：`.env` 文件已被 `.gitignore` 排除，不会提交到版本库。请勿将 API Key 或 SECRET_KEY 硬编码在源码中。

### 7.4 启动应用

```bash
python app.py
```

启动后访问 `http://127.0.0.1:5000`。数据库 `exam_system.db` 会在首次启动时自动创建和初始化。

### 7.5 生产部署建议

- 将 `app.run(debug=True)` 改为 `app.run(host='0.0.0.0', port=5000)`
- 使用 Gunicorn 或 uWSGI 作为 WSGI 服务器
- 使用 Nginx 做反向代理和静态资源托管
- 将 `SECRET_KEY` 改为强随机字符串（至少 32 位）
- SQLite 适用于中小规模；大规模并发建议迁移至 PostgreSQL/MySQL

---

## 八、路由总览

| 方法 | 路由 | 蓝图端点 | 权限 | 功能 |
|------|------|----------|------|------|
| GET | `/` | `main.index` | 公开 | 首页 |
| GET/POST | `/register` | `auth.register` | 公开 | 注册 |
| GET/POST | `/login` | `auth.login` | 公开 | 登录 |
| GET | `/logout` | `auth.logout` | 登录 | 登出 |
| GET | `/dashboard` | `main.dashboard` | 登录 | 仪表盘（按角色分流） |
| GET/POST | `/add_question` | `teacher.add_question` | 教师 | 添加题目 |
| GET | `/manage_questions` | `teacher.manage_questions` | 教师 | 题库管理 |
| GET/POST | `/edit_question/<id>` | `teacher.edit_question` | 教师 | 编辑题目 |
| POST | `/delete_question/<id>` | `teacher.delete_question` | 教师 | 删除题目 |
| POST | `/generate_explanation/<id>` | `teacher.generate_explanation` | 教师 | AI 生成单题解析 |
| POST | `/generate_all_explanations` | `teacher.generate_all_explanations` | 教师 | AI 批量生成解析 |
| GET/POST | `/create_exam` | `teacher.create_exam` | 教师 | 创建考试 |
| POST | `/toggle_exam_status/<id>` | `teacher.toggle_exam_status` | 教师 | 启用/禁用考试 |
| GET | `/exam_results/<id>` | `teacher.exam_results` | 教师 | 查看考试统计 |
| GET/POST | `/grade_subjective_questions` | `teacher.grade_subjective_questions` | 教师 | 评分复审 |
| POST | `/clear_exam_scores/<id>` | `teacher.clear_exam_scores` | 教师 | 清空考试成绩 |
| POST | `/delete_exam/<id>` | `teacher.delete_exam` | 教师 | 删除考试 |
| GET/POST | `/ai_generate` | `teacher.ai_generate` | 教师 | AI 智能出题 |
| GET | `/take_exam/<id>` | `student.take_exam` | 学生 | 参加考试 |
| POST | `/submit_exam/<id>` | `student.submit_exam` | 学生 | 提交考试 |
| GET | `/exam_result/<id>` | `student.view_exam_result` | 学生 | 查看考试结果 |
| POST | `/cheating_detected/<id>` | `student.cheating_detected` | 学生 | 防作弊事件上报 |
| GET | `/image_attachment/<id>` | `common.get_image_attachment` | 登录 | 获取图片附件 |

---

## 九、安全设计

| 安全措施 | 实现方式 |
|----------|----------|
| 密码哈希 | Werkzeug PBKDF2 加盐哈希，数据库不存明文 |
| 会话管理 | Flask session + `secret_key`，登录状态存于服务端 session |
| 登录保护 | `login_required` 装饰器，未登录跳转 |
| 角色鉴权 | `role_required` 装饰器，教师/学生权限隔离 |
| 越权防护 | 考试结果查看强制绑定当前用户 ID；题目编辑/删除校验 `teacher_id` |
| 编程题沙箱 | 子进程隔离 + 受限环境变量 + 5秒超时 + `-S` 禁用 site 模块 |
| API Key 保护 | `.env` 文件 + `python-dotenv` + `.gitignore` 排除 |
| SECRET_KEY 保护 | 从 `.env` 读取，无硬编码回退值 |
| 防作弊 | 前端切屏/失焦检测 + 行为日志写入数据库 |
| SQL 安全 | 参数化查询（`?` 占位符），无字符串拼接 SQL |

---

## 十、日志系统

系统使用 Python 标准库 `logging`，配置如下：

```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s [%(levelname)s] %(message)s',
    datefmt='%Y-%m-%d %H:%M:%S'
)
```

**关键日志点**：
- 数据库迁移（新增列）
- 编程题评测超时/运行错误/执行异常
- AI 评分出错
- 防作弊日志写入失败（记录不存在/JSON 解析失败）
- AI 生成解析失败
- 清空成绩/删除考试失败

---

## 十一、已知限制与后续优化方向

| 项目 | 当前状态 | 优化方向 |
|------|----------|----------|
| 数据库 | SQLite 单文件 | 高并发场景迁移 PostgreSQL |
| 文件上传 | 图片存数据库 BLOB | 迁移至对象存储/文件系统 |
| 编程题安全 | subprocess + 受限环境 | 接入 Docker 容器沙箱 |
| AI 评分 | 单次调用，无重试 | 增加重试机制和超时控制 |
| 前端 | 服务端渲染 Jinja2 | 可拆分为前后端分离架构 |
| 并发 | Flask 开发服务器 | 生产环境使用 Gunicorn + Nginx |
| 题目复用 | 题目仅归属创建教师 | 可增加公共题库/跨教师共享 |